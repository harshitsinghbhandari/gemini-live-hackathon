---
trigger: always_on
---

Always find a python environment before executing any python file. I usually keep it env/ or venv/ or .venv/ as my environment. I never run on python as global. always use virtual envirnoment don't create new just find what's there